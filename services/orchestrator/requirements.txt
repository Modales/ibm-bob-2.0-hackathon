# Orchestrator service dependencies
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27

# Optional heavyweight backends — every module runs standalone without these,
# but installing them upgrades mocks to the real thing:
#   sentence-transformers>=2.7   # semantic CVE embeddings (vector_cve_db.py)
#   faiss-cpu>=1.8               # ANN index for large CVE catalogs
